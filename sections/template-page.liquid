<div class="flex flex-col items-center my-8">
    <h1 class="text-3xl font-medium my-8 md:my-8">{{ page.title }}</h1>
    <div class="max-w-6xl mx-auto px-4 text-justify">{{ page.content }}</div> 
</div>
